<h1> All users </h1>


<%= render 'shared/search_form' , :path => users_path,  :lable => 'Users' %>



<%= will_paginate %>

<ul class="users">
	
	
	<%= render @users %>
	
	
</ul>

<%= will_paginate %>