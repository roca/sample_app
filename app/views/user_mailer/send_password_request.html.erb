<p> <%= @user.name %> </p>

<p>Password Request</p>

<p>Your email is                : <%= @user.email %></p>
<p>Your new activation token is : <%= @user.activation_token.token %></p>


This token will expire after it's first use.

<%  @domain = Rails.env == 'development' ? 'localhost:3000' : 'westwoodvelo.org'%>

<p><%= link_to "Sign in with token now!", url_for( :action => 'token', :controller => 'sessions', :host => @domain ) %></p>